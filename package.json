{
  "name": "untitled",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "build:pages": "npm run build && rm -rf index.html assets && cp dist/index.html ./index.html && cp -R dist/assets ./assets",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "sass": "^1.99.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.12"
  },
  "private": true
}
